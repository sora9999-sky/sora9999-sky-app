Place a 256x256 (or 512x512) Windows icon file here named `icon.ico`.

If you skip this, Electron will use its default icon.

Free tools to make one:
- https://icoconvert.com/  (convert any PNG to .ico)
- Or simply rename a `.png` to `.ico` for testing (lower quality).
